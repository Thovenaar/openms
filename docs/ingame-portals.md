# Original portal reconstruction

## Scope and provenance

This is an offline reconstruction from the supplied `Maplestory_UNPACKED.exe` and WZ files. There is no original recording, original C/C++ source, Windows execution or Nexon server. Authorized Cosmic server-reference code is not original client evidence or Nexon source. Packaged traversal is **not server authorization**. Neither this implementation nor its smoke evidence establishes visual parity with the original running game.

Implementation: [`portal-data.js`](../client/tools/portal-data.js), [`portal-system.js`](../client/src/world/portal-system.js), [`field-transition.js`](../client/src/world/field-transition.js) and the existing Main atomic scene owner. Original portal investigation used the isolated `/tmp/maple-ingame-portals` Ghidra project and installed original-binary `analyzeHeadless` runner with `docs/tools/clientFocus.java`; later address-directed transition exports are indexed in [transition-contract.json](ghidra-client-corrections/transition-contract.json). Retained outputs:

- [`consumers.txt`](ghidra-ingame-portals/consumers.txt): all seven assigned functions (`00712d35`, `0071332c`, `00712313`, `0071259d`, `00712827`, `0071165a`, `0053185c`), plus original rectangle selectors and record helpers.
- [`activation.txt`](ghidra-ingame-portals/activation.txt), [`input-timing.txt`](ghidra-ingame-portals/input-timing.txt): Up activation, request admission, same-map handling, frame loading and conversion.
- [`ranges.txt`](ghidra-ingame-portals/ranges.txt), [`collections.txt`](ghidra-ingame-portals/collections.txt), [`frame-default.txt`](ghidra-ingame-portals/frame-default.txt): exact instructions resolving decompiler-damaged arguments, collection ownership, omitted defaults and selected variant.
- [`automatic.txt`](ghidra-ingame-portals/automatic.txt), [`automatic-range.txt`](ghidra-ingame-portals/automatic-range.txt): actual automatic/script/impact consumers; these are evidence, not claims that the browser runs them.
- Lossless original metadata: [`Map.tar.gz`](ingame-inventory/Map.tar.gz), per-map `portal.json` members. MapHelper IMG SHA-256: `ea1cf9eeaf6b35809cfca421337e1d6f49ac1f4b2d6852a59f67063653cdf013`.

The loader's string `0xeba` is **hideTooltip**, not script. WZ `script` is retained raw; the portal request transmits the original portal name rather than interpreting JavaScript or implementing server scripts.

## Recovered graphics and timing

`00712313` reads `Map/MapHelper.img` → `portal` → `game` → `pv` (IDs `0x6b0`, `0x5c1`, `0x6b1`, `0x6af`). The loader installs this looping artwork for types 2, 4 and 7. Its animation call uses mode `0x20`.

`0071259d` selects `ph` (`0x1122`); `00712827` selects `psh` (`0x1123`). `00712d35` uses `ph` for type 10 and `psh` for type 11. The loader places types 10/11 in the manager's `+0xc` collection and stores their `image` variant. Only **empty or absent image** is replaced by string ID `0x3c0`, `default`; an unknown nonempty variant must not fall back silently. The eight supplied type-10 records all use this empty/absent default.

When the selected nearby hidden portal changes, `00712d35` starts the old portal's `portalExit`, clears its current/start layer, and starts the new portal's `portalStart`. Both use mode zero. `0071332c` checks `00443e36()==0` to release a completed Start, install `portalContinue` with mode `0x20`, and release a completed Exit. The runtime mirrors those one-shot/loop transitions for all supplied hidden portals. It does not reverse an in-flight Start animation when departing; it starts Exit.

**Type-11 recovered family:** `0071332c` calls the **ph helper unconditionally** for Continue even though Start/Exit select psh. Extraction now reproduces that exact mixed family; it does not substitute psh Continue. A missing requested ph/psh variant is an explicit original-artwork dependency failure, never a default-variant fallback.

All these graphics call `0043ea3e`; its numeric-frame loop calls `0043f768`. The latter pushes **`0x78` (120 ms)** before converting frame `delay`, string ID `0x15f8`, through `00414d40`. This provides a real original-consumer chain for the portal default; it is **not** inferred from the previous shared extractor's 120-ms policy. Portal extraction explicitly normalizes numeric-string delays, retains explicit nonnegative delays, original origins and alpha endpoints, and rejects negative or nonintegral milliseconds. Equal-time zero frames follow the recovered Gr2D queue semantics documented in [client evidence](client-evidence.md#timed-frame-alpha--verified).

| Original artwork         | Frames | Duration |
| ------------------------ | -----: | -------: |
| pv loop                  |      8 |   960 ms |
| ph/default Start         |      4 |   480 ms |
| ph/default Continue loop |      7 |   840 ms |
| ph/default Exit          |      3 |   360 ms |

Portal depth is original `0xc0041f78`, normalized by the same common `B=-0x40000000` removed from existing map/entity depths: **270200**, between ordinary world layers and front backgrounds. Equal-depth ordering remains the existing stable extraction policy, not proved original tie behavior. Editor portal icons are never substituted for game artwork; the executable's `game/pg` string has no corresponding supplied artwork.

## Actual input, geometry and request semantics

- `0094c856` dispatches virtual key **`0x26` (Up)** to `0094df9b`.
- `0094df9b` calls `00712ab1` with half-width **20**. It searches ordinary non-type-zero records in reverse source order, using the original point-in-rectangle operation on **`[x-20,x+20) × [y-50,y+50)`**. Type 6 is removed from this collection by the loader. The browser uses truncated integer simulation feet, not sprite overlap.
- That caller requires the movement unit's `+0x110` contact pointer. `009b1719` demonstrates that this is the foothold contact and copies its plane/group; the browser requires `simulation.footholdId`. Additional original action/status/server gates are not all reconstructed. The local browser attack action is blocked; absent server-owned statuses are not invented.
- `00950555` uses half-width **100** with `00712c57` on the separate type-10/11 collection. This is **`[x-100,x+100) × [y-50,y+50)`**, not the narrower Up-entry rectangle. It supplies the nearby portal to the hidden-graphics transition routine. Browser presentation reevaluates this selector each active update; the original invokes the callback when either of its two arguments is nonzero. Precise original callback scheduling/attachment on initial stationary entry remains unverified.
- Ordinary cross-map `0053035d` admits a request only while the global pending flag is clear and elapsed time since the previous timestamp exceeds **499 ms**. It sets the flag and timestamp when transmitting. `PortalTravelGate`, owned by persistent `InGameSystems`, now retains both pending-token ownership and the interval across successful scene replacement. `0053185c` timestamps the response, including failure; local success/failure/cancellation settlement likewise starts the next 500-ms interval. A stale token cannot release a newer request. `performance.now()` supplies one monotonic application clock; inactive-tab wall time is explicitly browser policy.
- Same-map non-type-4/5 routes take `00957b74`. **Fresh recovery corrects the earlier 120-ms-delay interpretation:** request stores the destination at `+0x2b20/+0x2b24`, request time at `+0x2b28`, and an initial admission deadline `now+120` at `+0x2b2c`. [`0094e5e5`](ghidra-ingame-portals/entities-motion.txt) moves on the next update when `now >= +0x2b28`, then replaces `+0x2b2c` with **now+600**. The browser therefore does not invent a 120-ms travel pause; the shared gate retains the 600-ms same-map recovery after successful commit. Type 4/5 always take the packet-admission path even when target map equals source.
- Original destination entry `00949513` subtracts **10** from selected portal y. The integration must spawn at the exact named destination `(x,y-10)`, not at source coordinates or a random/default spawn.
- After accepted non-type-4/5 ordinary requests, `0094df9b` resolves string ID `0x15ba`, **Portal**, for the Game sound path. Types 4/5 suppress that sound. `00957b74` resolves `0x884`, **Effect/BasicEff.img/Teleport**, for same-map motion. Main receives these exact sound/effect choices with the travel token and dispatches them only after an accepted atomic offline commit. Moving the original request-time effect/sound to accepted completion is deliberate offline failure safety, not original response latency.

The browser latches an **Up keydown edge** independently of held Up, consuming it once before physics. A keydown+keyup within one render interval is therefore not lost. Focus/map replacement clears held and pending edges; subsequent OS repeat cannot rearm until a fresh physical press. This is browser duplicate/bounce safety, not an assertion of identical Windows repeat scheduling. Holding Up or pressing again while loading cannot create a second request. Rejection retains the old scene; generation/destroy guards prohibit stale completion/error mutation.

## Field transition versus same-map teleport

The [transition contract](ghidra-client-corrections/transition-contract.json), [native instructions](ghidra-client-corrections/transition-native.txt) and [original WZ probe](ghidra-client-corrections/transition-wz.json) distinguish two paths; a portal cooldown is not a fade duration.

- **Ordinary field replacement:** `005294ce → 0063988a → 00776e65` reduces native global RGB brightness with `trunc((600−elapsed)*255/600)` for600ms, then sets zero. `00529269 → 00776b64` schedules the ordinary incoming rise to255 over600ms. The outgoing loop is synchronous; incoming fade is scheduled while user construction/gameplay continues. The browser freezes source simulation/input through leaving/loading/covered, prepares the candidate asynchronously, and commits only when both the candidate and black boundary are ready. Input/physics are released at commit/reveal, **not after a second600-ms lock**. Destination movement continues during the600-ms reveal.
- **Same-map non-type-4/5 motion:** relocation occurs on the next eligible update without scene teardown, an asynchronous window-close boundary or global fade. It synchronizes previous/current actor positions at the exact named `(x,y−10)` arrival and preserves field systems, fixed clock, BGM and camera-filter history. Two preloaded original `Effect/BasicEff.img/Teleport` animations restart at the departure and arrival positions: **four80-ms frames,320ms total**, independent of600-ms admission recovery. The provisional120-ms guard is not a travel pause. Types4/5 retain the packet/field path even when map IDs match.
- **Camera:** `00437a31`, with `00437b32/00437b35`, attaches the local-user target at `(x,y−50)`. The recovered Shape2D filter now replaces the earlier immediate-follow implementation; see the filter contract below. `00641ef1` clips the filtered center to `VRLeft+400`, `VRRight−400`, `VRTop+300`, `VRBottom−300` at 800×600, collapsing nonpositive ranges to their midpoint. Other viewport sizes generalize the half-extents as browser policy. A new scene's first filter commit snaps; a same-map relocation retargets the existing filter and therefore visibly eases across the field rather than snapping the camera.
- **Failure/cancellation:** the old complete scene remains owned until accepted commit. Failed replacement restores it and reverses darkness from the current value at the same fade rate; no successful travel sound/effect is emitted. Stale request IDs cannot reveal or cancel a successor. Ordinary developer map selection/reload is an immediate browser path and cancels prior presentation transitions. Revival uses field replacement/fade even when the authored return map has the same ID.

`FieldTransition` owns only bounded presentation state and opaque/visible boundaries; Main owns scenes, abort signals, profile commit and rollback. A full-viewport black overlay above field/HUD approximates native global RGB modulation without changing scene alpha. Async candidate preparation, hidden-tab suspension and completion-time sound/effect dispatch are deliberate browser policies. The [historical native-input report](ingame-validation/interaction-corrections/report.json) exercises its recorded integrated paths; no original Windows parity is claimed.

### Recovered camera-filter dynamics

[`camera.js`](../client/src/rendering/camera.js), the [EXE attachment/filter export](ghidra-client-features/camera/camera-exe-focus.txt), and [original Shape2D DLL decompilation](ghidra-assets/Shape2D.dll/decompiled.c) retain the distinction between the target vector, its filtered public coordinates and clipped viewport. Shape2D `51408d23/51408e33` use filter `0x1e0000`, coefficient 100 and a 28-pixel deadband. For one axis, with distance `D` from the committed history to the integer target and signed elapsed renderer milliseconds `dt`, the remaining distance is `D` inside the deadband, otherwise `max(0, 100000*D/(dt*D+100000))`. The output approaches the target by subtracting that residual with the original direction. Coefficient 100 is **not a 100-ms tween duration**.

Default clipping must use the **physical-space bounds**, not raw foothold extrema. `00641ef1` obtains the rectangle from `00a43e7b`; the [world-bound loader](ghidra-physics-refinements/world-bounds-loader.txt) first applies x+30/−30 and y−300/+10 plus original VRLimit restrictions. Camera expansion then adds left−20, right+20, top−60, bottom+100, with each explicit VR edge overriding its corresponding default. `camera.js` shares `prepareBounds` with physics. Omitting the physical top margin caused upper-platform characters to reach the screen edge even though filtering itself was correct.

Committed history stays double precision; public coordinates truncate to signed integers. Read-only probes do not initialize or commit, and retargeting does not reset history. The [first-pass native gameplay report](native-ui-validation/gameplay/report.json), case `same-map-portal-camera`, traversed actual `hp01→hp01_1`: arrival `(5801,444)` preceded floor settlement `(5801,454)`, global opacity stayed zero, and its 108 consecutive post-relocation X comparisons matched that report's source filter. This is browser/source numerical evidence, not Windows runtime equivalence. That pass did not capture reciprocal travel or each Teleport frame; the older interaction report's four-frame capture belongs to its own earlier build.

The [final native gameplay replay](native-ui-validation/final/gameplay/report.json), source `eb84f90…`, traverses **both `hp01→hp01_1` and `hp01_1→hp01`** through native Up. It records two requests/completions, each600-ms same-map recovery, named arrival/floor settlement, zero global opacity, visible camera travel and actual Teleport pixels. Public observations traverse frames0,1,2,3 then no active effect in both directions. These are wall-clock samples and motion captures, not separate screenshots of each frame or proof of exact80-ms browser scheduling. The final pass does not repeat the first-pass numerical camera comparison or claim Windows parity. It separately confirms authored return-map revival with600-ms leaving/reveal and input released at commit; failure/auto-confirm paths remain historical or unexercised.

Family travel is a distinct local transaction, not ordinary Up-portal activation. The [final Family report](native-ui-validation/final/family/report.json) on its exact `eb84f90…` source exercised native reunion, outgoing summon with visible peer consent, and incoming native receiver travel to a unique portal destination, with held input cleared at modal admission/commit. It also retained a concrete failure: incoming summon to Henesys rejected `Ambiguous destination portal sp`, preserving the source and invitation. Selecting authored portal ID0 must not be confused with proving unique portal-name routing; that report does not claim the duplicate-name destination works.

## Automatic and server-owned behavior: evidence without invention

The automatic selector is not the Up selector. `00712b61` searches the manager's `+8` collection (types 3, 9, 12, 13) in reverse order, using **half of hRange/vRange**, truncating integer division. Loader instructions prove omitted **hRange=100**, **vRange=100**, and **delay=0**. These fields are not substitutions for the Up-entry or hidden-reveal constants.

`0094dac6` type **3** follows the ordinary destination branch: sentinel refusal, exact same-map name via `00957b74`, otherwise `0053035d`, and Game/Portal sound. Automatic same-map calls bypass the ordinary foothold requirement; type3 is now enabled without an Up edge using the recovered hRange/vRange rectangle. The browser makes at most one failed attempt per continuous overlap, requiring exit/reentry after a dependency failure; cooldown-only refusal remains eligible once time elapses. That failure rearming is explicit browser policy, not recovered packet retry timing.

The type-9 branch passes authored delay to `00485bf7`, gates `onlyOnce` against user `+0x31b4`, and sends a script-packet request. Untranslated type9 scripts remain `automatic-script-packet-unavailable` or `server-script-unavailable`. Type12 invokes an impact-skill path; type13 also tests field kind, reactor state and contact. These remain classified `impact-skill-state-unavailable` and `impact-field-reactor-state-unavailable`; no guessed impulse or script reward is installed. Exact tutorial and Free Market translations below are the only admitted script authorities.

Nonscript named ordinary routes now admit types **1,2,3,4,5,7,8,10,11**; type3 additionally activates automatically. The ordinary consumer uses named routing for nonsentinel type7/8/11 too; only their sentinel branches are script requests. Type0 is spawn-only and type6 remains special-loader metadata. `onlyOnce`, `delay`, impacts and `reactorName` are retained, but their type9/12/13-specific consumers are not incorrectly applied as gates to ordinary type1/2/3 routing. Dead or locally action-blocked actors cannot request travel, while portal graphics continue.

`0053185c` is a response handler, not activation logic: it clears the global pending flag, updates the timestamp, consumes a response code, and selects messages including **“The portal is closed for now.”** This is direct evidence that raw `tm`/`tn` is not permission.

### Offline beginner tutorial trigger

Original `Map.wz:Map/Map0/000010000.img/portal/4` is named **tuto00**, but its script is **tutoChatNPC**: type9, `(−95,428)`, sentinel `tm=999999999`, empty `tn`, `onlyOnce=1`, `delay=0`. The original spawn portals are around `(−93,450)`, inside its automatic range. The reported failure occurred on this automatic overlap, not an attempt to travel through a portal named `tuto00`.

Authorized `Cosmic/scripts/portal/tutoChatNPC.js` (SHA-256 `a3a95daa99e103a322871508d5f5f590792d8cf7a9346f745751c2ba31775169`) is now the fourth exact complete program admitted by `npc-script-portals.js` / `compileTutorialPortal`. It calls `pi.openNpc(2007)` **only when `pi.hasLevel30Character()` is true**, then calls `pi.blockPortal()` and returns true on either branch. A new level1 account therefore silently completes and blocks the tutorial trigger without dialogue or movement. This is the authored condition, not a bypass of arbitrary script validation. Missing or altered source programs still fail admission.

`Cosmic/src/main/java/scripting/portal/PortalPlayerInteraction.java:53–69` queries all characters of the same account for level≥30 and includes the current character. Offline authority maps that query to the bounded, validated browser-local character roster, using the active profile for its own row rather than a stale saved level. Temporary development profiles remain isolated and use only their own level. Unreadable roster rows are explicit failures, never treated as proof of ineligibility. This local-account mapping is browser policy, not original client behavior.

The admitted `openNpc` path reuses the compiled NPC session, original portrait/native Yes/No window, busy/live-field guards and atomic NPC travel owner. It is a script-owned NPC lease, not a fabricated visible world placement or permission to click hidden NPCs. `AbstractPlayerInteraction.java:358–365` retains an already-open conversation instead of replacing it. Authored `Cosmic/scripts/npc/2007.js` asks whether to skip to Lith Harbor; **Yes** calls `cm.warp(104000000,0)`, **No/Close** disposes without movement. The destination is numeric portal0, not an arbitrary portal named `sp`: original `Map.wz:Map/Map1/104000000.img/portal/0` is `(−495,−110)`, so arrival is `(−495,−120)` before physics settles. Portal-opened NPC dependencies now enter the existing bounded map closure even without a map `life` placement. Destination preparation/asset failure retains the last complete source scene and the current NPC turn.

NPC2007 also exposed a compiler defect: the static output checker combined maxima from sibling conditions even though `status--` makes the refusal and offer mutually exclusive. Conditional fallthrough now combines only guaranteed output counts; the existing VM remains authoritative for the actual callback path and rejects a second view before any draft commits. Statements after `dispose` still execute, matching the existing server semantics; there is no NPC-ID/source exception or invented early return. The exact authored NPC2007 program is compiled normally, including its finite Lith Harbor dependency.

The session block is installed only after the local trigger operation succeeds. NPC-opening failure leaves it unblocked and uses the existing failed-overlap exit/reentry and response cooldown policy; cancellation cannot open a stale scene's conversation. No direct travel, reward, level grant or original portal sound is fabricated by the trigger. Other beginner scripts, including `glTutoMsg0`, remain explicit unsupported authority.

Verification recipe after regeneration: launch the actual browser with a fresh level1 local character in `000010000`, allow spawn overlap without pressing Up, and confirm no `server-script-unavailable`, no NPC window, unchanged scene and a settled portal gate. With a second real local character at level30 (clearly label any editor fixture), reload a fresh beginner session: spawn overlap must open NPC2007. Exercise No/Close separately from Yes; Yes must use the faded atomic NPC path to `104000000` portal0. Repeat with destination assets unavailable to confirm the current dialogue/source survive without changing durable location, then with a complete cached release and transport disabled. The focused command `bun test client/test/npc-script-runtime.test.js client/test/portal.test.js` passed **39 tests /181 assertions**, including exact NPC2007 No/Yes behavior and atomic rejection of a dynamically selected double-dialog path. Original WZ and server-source inspection also executed. Browser acceptance and full extraction remain integration-pass evidence, not claims established by these focused tests.

### Offline Free Market routes

Native sentinel types7/8/11 admit exactly Cosmic `market01`–`market24`, `market26`, `market52`–`market56` for entry and `market00` for return. This is authorized server-reference policy translated to local authority, not execution of arbitrary scripts or Nexon server code. Entry saves the numeric source map in [schema8](offline-profile.md#schema-8) `savedLocations.FREE_MARKET` and arrives at `910000000/out00`. Return selects the saved map's authored market portal by numeric ID, then clears the saved return. Only an absent saved return uses the original script's `100000000`/portal0 fallback; asset/network failures never substitute a different map.

Main prepares the destination before atomically committing location and saved-return state, retains the source through failure and rolls back a durable location change if publication fails. Reload inside Free Market preserves the return. The bounded extraction closure includes Free Market and its rooms while retaining per-map offline caching and the1024-map limit. [Inspection-root selection](asset-delivery.md#original-selection-evidence) separately includes source-backed beginner and jump-course scenes without authorizing their blocked entry/reward scripts. Native acceptance traversed `100000100/market00` → `910000000/out00`, reloaded, then returned to original `(838,155)` portal21 with saved return cleared.

### Authored NPC travel and World Tour

NPC travel is a separate compiled callback path, not permission to execute raw portal scripts. `npc-script-compiler.js` admits `cm.getPlayer().saveLocation(type)` only for one literal key from the shared [schema8 saved-location inventory](offline-profile.md#schema-8). `npc-script-authority.js` stores the actual current numeric map; `peekSavedLocation` leaves a present slot intact and `getSavedLocation` consumes it. Both return−1 for an absent slot, leaving any fallback to the authored script. No portal object is persisted.

The supported World Tour callbacks retain their authored finite destination/fee/portal arrays and branches: Spinel9000020 saves `WORLDTOUR` before outbound travel; Irene9201135 peeks for return-menu text and consumes the saved map for return. The Malaysia route connects541000000 and550000000 through those callbacks, rather than a synthetic menu or preseeded saved return. Main prepares the destination before the callback's atomic fare/saved-location/location publication. `Character.changeMap`'s authored NPC portal-zero fallback is separate from strict named ordinary portal routing. Failed preparation does not spend the fare or consume a return; a saved ID alone does not establish destination availability.

`sendDefault()` is another narrow compiler translation: zero arguments and the routed NPC's original String.wz `d0` text become the existing `sendOk` dialogue operation. Missing text blocks compilation; no generic replacement prose or arbitrary JavaScript execution is added. Current release selection follows original inspection roots, admitted strict portal edges and finite supported-NPC map dependencies, bounded to1024 maps. Packaging a beginner, jump-course or World Tour scene does not implement every entry/reward script or all course/quest mechanics. Exact native/offline outcomes and fixture provenance belong to [validation](validation.md).

## Coverage: every one of the 131 records

The complete per-record presentation accounting is retained in [`extraction-smoke.json`](ghidra-ingame-portals/extraction-smoke.json). It maps each original portal index to a visual entity or explicit metadata-only entry without creating a second runtime route convention.

| Map       | Records | pv entities | ph entities |
| --------- | ------: | ----------: | ----------: |
| 100000000 |      34 |           7 |           4 |
| 100000001 |       2 |           1 |           0 |
| 103040000 |       4 |           1 |           0 |
| 108000500 |       1 |           0 |           0 |
| 120000000 |      17 |           1 |           4 |
| 200090500 |      48 |           1 |           0 |
| 211040000 |       7 |           3 |           0 |
| 230000000 |      18 |           4 |           0 |
| **Total** | **131** |      **18** |       **8** |

These historical eight-map totals are 0→23, 1→17, 2→16, 3→9, 6→18, 7→2, 9→38, 10→8. All105 nonvisual records remain represented. `highposition` and `undodraco` remain explicit unsupported requests. `market05` is now covered by the exact Free Market translation above; it is not an invented raw sentinel map edge.

Available cross-map pair: `100000000/in02` (original index 25, x5136/y453) ↔ `100000001/out02` (index 1, x202/y242). Same-map hidden pairs: `100000000 hp00↔hp00_1`, `hp01↔hp01_1`; `120000000 nt00↔nt01`, `cp00↔cp01`. Type-1 same-map routes include `120000000 pt00→pt01→pt02→pt00` and `200090500 st00→st00`. All other unavailable target maps must be rejected by catalog lookup; sentinel `999999999` is never a map or fallback.

## Integration and bounded ownership

`await extractPortals(context,map,mapId)` returns `{entities,presentation}`. Main appends entities with final global order and stores `manifest.portalPresentation`. Schema1 records retain `portalId`, `entityId`, graphics status and activation classification. Routes and all original descendants remain solely in `manifest.physics.portals` / `manifest.physics.map.$portalProperties`.

`new PortalTravelGate(clock?)` belongs to `InGameSystems`, never a scene. `tryBegin(packetPath)` returns a unique token or null; `complete(token,outcome)` settles only that exact active token; `cancel(token)` explicitly aborts application-owned work. `PortalSystem(scene,{travel,travelMarket,onError,travelGate})` requires that persistent gate. `handleInput(input)` consumes the latched native Up edge before physics; `update(ms,input)` also handles automatic selection and artwork. `.blocksMovement` exposes pending same-map motion. Scene destruction clears artwork references but **does not cancel or release a travel token**: successful Main commit destroys the old scene before its promise resolves. `finishTravel` always settles the token in `finally`, even after old-owner destruction, without stale UI/error callbacks.

`travel(mapId,targetPortalName,options)` receives `{token,signal,sound,effect,sameMapMotion,notBeforeMs,transition}`. `transition` is `"field"` or `"teleport"` according to the packet path; `notBeforeMs` is admission time, not a120-ms delay. Main validates catalog existence, exact destination-name uniqueness and generation/signal ownership, prepares the candidate, and waits for the opaque boundary on field transitions before committing atomically. Same-map motion relocates the existing scene. Game/Portal iff `options.sound`, and Teleport iff the effect is present, dispatch only after accepted commit. Field reveal does not hold the travel promise until fully visible. Signal cancellation belongs to superseding application work, never automatic source teardown. Hooks are synchronous nonthrowing notification boundaries except the awaited travel promise.

`collectPlayableMaps(context,seeds)` is a bounded synchronous WZ-driven closure export. It shares `portalRouteStatus` with runtime; each ordinary admitted edge must resolve a unique authored destination. Supported NPC routes add their finite map dependencies, while dynamic saved returns refer to admitted source fields. It returns `{ids,blocked}` and fails explicitly at1024 maps. The retained [route closure](ghidra-ingame-portals/route-closure.json) is historical: discovery from its eight seeds reached **356 maps**, including **57 reactor maps /26 reactor templates**, and distinguished the28-map direct neighborhood from full reachability. Its malformed `680000004/4 → 104040002/st02` edge had zero matching destinations and remained blocked. Those totals are not current release counts; explicit selected-map releases are not complete closure claims.

Portal art remains in package-v2 demand-loaded regions using existing atlas identities/budgets. The owner detects changed region entity identities and retains no leases. Engineering bounds:4096 portal records/map,1024 frames/action and1024 closure maps; no per-tick arrays, display objects, textures, or recursive route preloads. Hidden Start/Exit use the renderer's nonlooping playback and Continue loops. Reactor ownership and exhaustive remaining entity schemas are documented in [ingame-entities.md](ingame-entities.md).

## Executed proof and outstanding acceptance

The [historical interaction report](ingame-validation/interaction-corrections/report.json) records native Henesys↔Maya travel, black-boundary replacement, the 600-ms reveal with admitted movement, all four same-map Teleport frames without a global fade, and revival through the same field transition owner. A real portal request with an intentionally failed 503 manifest retained Henesys and reversed opacity from 0.392 to 0; service-worker bypass made the network fault effective. A separate native map selection superseded a portal at opacity 0.224, leaving zero pending loads and no stale black overlay. These are controlled browser failure tests on the recorded build, not final-build coverage or original server behavior. The newer [diagnostic gameplay pass](native-ui-validation/gameplay/report.json) additionally proves the implemented same-map camera filter and confirmed return-map revival on its stated source identity.

The domain extraction smoke ran the actual new extractor against original WZ parsed/decoded by existing `WzArchive`, `parseImage`, and `decodeCanvas`, with a small in-memory Frame/Part service adapter. It produced **131 presentation records, 26 entities, 22 unique pixel hashes, 1,277,980 unique decoded RGBA bytes**, and the exact durations above. The adapter does not prove shared atlas packaging or rendered pixels.

The scoped runtime smoke used original map physics and the real fixed-step simulation: entering at source portal index25's `(5136,443)` settled after 60×30 ms at **(5136,454), foothold189**. Up requested exactly **100000001/out02**; held Up and a second edge while pending did not duplicate the request. Injected travel failure retained the same scene and reported an error; a separate request rejected after destruction produced zero stale errors. Real `EntityAnimation` with dimension-only TextureSource objects exercised **Start→Continue→Exit→hidden** at 480/360 ms. Evidence: [`runtime-smoke.json`](ghidra-ingame-portals/runtime-smoke.json). This was a rendererless smoke with synthetic input state, not real keyboard/browser or streamed-commit acceptance.

The investigation executed against all original Reactor/Morph/TamingMob images and the complete supported map graph; retained decompilation includes `entities-motion.txt`, `reactor-events.txt`, `reactor-triggers.txt`, `reactor-hit.txt`, and `reactor-layers.txt`. These are evidence runs, not browser proof. That historical integration passed format, strict lint,140 tests/822 assertions and extraction. Neither this gate nor the later interaction report is a new final-source validation or exhaustive type3/type11/all356-map gameplay acceptance.

The historical delegated native-input pass is [portals/acceptance.json](ingame-validation/portals/acceptance.json), with screenshots and real post-gain portal PCM. It exercised bidirectional named travel, held/repeat guards, pv looping, ph27 Start/Continue/Exit, hp01 same-map travel, sentinel/missing target refusal, cooldown and latest-selector races. First committed destination was `(5136,443)`, then settled at `(5136,454)`. [lifetime-and-short-tap.json](ingame-validation/lifetime-and-short-tap.json) retains Main's failed-before/passed-after short-tap and shared-HUD-lifetime reproduction. Other hidden pairs, early-Start interruption, scripts/impacts and injected network failures were explicitly outside that pass. It is not original Windows parity or a performance measurement.

## Expanded original Windows capture request

Obtain original runtime video plus frame-step screenshots and input timestamps for: ordinary Up entry at rectangle edges while grounded/airborne/attacking; Windows auto-repeat across map changes; hidden reveal on stationary entry and proximity boundaries; Start interrupted by departure/reentry; simultaneous nearby portals; type11 psh variants and Continue family; same-map relocation on the next eligible update, its provisional120-ms guard and600-ms recovery; ordinary600-ms outgoing/incoming brightness and incoming input release; destination camera target/clipping and native vector follow dynamics; repeated rejected/accepted cross-map requests around500ms; type9 onlyOnce reset and authored delay; type12/13 impact timing/sign with reactor/field conditions; hideTooltip; type6 special data; missing game/pg; accepted/rejected server responses. Evidence-backed browser transitions do not replace these original-runtime comparisons.
