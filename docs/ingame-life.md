# In-game life: original metadata and local offline gameplay

Original metadata extraction and inspection use only the supplied `Maplestory-Client` WZ archives and `Maplestory_UNPACKED.exe`. The [offline combat authority](offline-combat.md) implements local mob movement, combat, HP/MP, death/recovery, respawn and progression; it is not original server reconstruction. Per-mob loot now consumes the authorized `/Users/k/Development/tensorfish/MapleStory-Server` Cosmic **server reference**, combined with original WZ item metadata/artwork and local durable pickup rules. Cosmic is not Nexon source or a third-party client implementation. Original evidence and historical preview verification below are retained; no original runtime recording, Windows execution or missing script is fabricated.

## Delivered modules and integration

- `client/tools/life-data.js`: `await extractLife(context, map, mapId)` returns `{entities, life}`. Append entities to the existing map entity collection and retain `life` as `manifest.life`.
- `client/src/life-system.js`: `new LifeSystem(scene, hooks)`, `refresh()`, `update(ms)`, `snapshot()`, `destroy()`.
- `client/src/life-geometry.js`: owned, preallocated current/swept mob-body and separate NPC-interaction geometry.
- `client/src/life-controls.js`: bounded, clearly labeled DOM preview controls; no shared HTML/CSS modifications.

`scene` supplies the shared scene contract plus persistent diagnostic `scene.overlays`. Main advances mob gameplay/artwork only in the fixed tick before `LifeSystem.update(ms)`, which observes mobs without changing their action, phase or visibility. `life.refresh()` follows membership changes. LifeSystem owns labels/listeners/diagnostic graphics, not textures. NPC artwork remains region-owned; nameplates attach to its native actor depth group, and are recreated only if region teardown destroyed that group. Mob artwork is separately demand-leased through `OfflineMobRenderer`, so region unloads cannot reset live mob state. Destroy LifeSystem before scene/entity/texture ownership.

Ordinary mob damage, hit pose and recoil have separate admission. Original `pushed` (absent default1) gates ordinary hit reaction; subthreshold damage still changes HP. Existing attack pose/pending impact survive admitted recoil, and hits during the separate active hit deadline do not restart or reverse recoil. Ordinary130-pixels/s recoil uses400-pixels/s² braking; recovered weapon chance selects the separate strong300/200 branch, not ordinary reaction eligibility. Connected-floor motion is not clipped to patrol `rx0/rx1`; flying/special controllers remain unsupported. Player-hit mobs do not inherit player protection blink: native mob blink requires `damagedByMob`. Player protection separately uses1500ms and two-of-four30-ms updates per gray/normal RGB cycle, not disappearance. See the [corrected combat contract](offline-combat.md) and [historical Hector/Red Snail evidence](ingame-validation/expanded/gameplay/report.json).

The separate player brace is now implemented, not merely an unused recovered deadline: admitted positive hits and ordinary/skill attacks write 5000ms; only grounded idle `stand1/stand2` substitutes original `alert` artwork. Walking, jumping, crouching, climbing, sitting and an existing attack keep their own pose. This is not a movement lock or an extension of the signed 1500-ms hit protection. Since `alert` is not an ordinary HP-recovery action, passive HP progress starts again only after admitted stationary standing; MP has an independent clock. See [the exact brace/recovery contract](offline-combat.md#recovered-natural-recovery).

The lethal transition emits `onKill(templateId,mob)` once, preserving quest accounting while supplying position/death identity to the sole `DropSystem` owner. The356-map release closure packages all4,305 relevant Cosmic source rows across157 mob templates and1,643 item IDs, with no unavailable rows after extraction. This is source coverage, not spawn/controller permission, item-use support or native pickup acceptance. Drops remain independent of streamed mob artwork: original floor placement, settlement, expiry, metadata-gated atomic pickup and rollback are documented in [per-mob drops](offline-combat.md#per-mob-drops-and-durable-pickup). Uncollected drops are transient field state; accepted credits are durable. LifeSystem/inspectors never award loot.

The [expanded historical gameplay report](ingame-validation/expanded/gameplay/report.json) proves its then-current Red Snail32/hit1, Hector798 versus800 recoil admission, retained attack1 pose and keyboard Talk to Maya. Those fixed damage recipes predate the recovered randomized sword generator and are not current setup guarantees. The later [native gameplay diagnostic](native-ui-validation/gameplay/report.json) observes Hector322 below `pushed800` versus907 with `hit1`/130px/s recoil, positive contact hits exactly1500 simulated ms apart, and independent brace/HP/MP clocks. It explicitly leaves exact799/800, repeated-hit refusal, retained pending impact and type2 native recoil unexercised. The [earlier interaction report](ingame-validation/interaction-corrections/report.json) retains Blue Snail loot, Cody scrolling, Maya's requirement refusal and Nella's once-only1000-meso quest transaction. These are scoped examples on their recorded builds, not all-template/all-quest coverage or final-source equivalence.

The [final native gameplay replay](native-ui-validation/final/gameplay/report.json) on source `eb84f90…` adds current322/907 below/above-threshold proof, and an admitted1112 hit with Hector still in `attack1` at frame4/actionMs660 and independent hit600/speed130. A separate trace observes130→118 over30ms. It also observes real contact/protection, later post-hit natural healing and learned Stance expiry followed by ordinary low-roll recontact. This is retained-pose evidence, not proof that the pending mob impact eventually damaged the player. Exact799/800, repeat-hit refusal, type2 and special-controller motion remain unexercised in that final browser report.

Additional public preview methods:

- `select(id)` selects a stable authored `life:<index>` placement.
- `setPreviewAction(id, action)` selects original NPC inspection artwork only; gameplay-owned mob actions cannot be overridden by the inspector.
- `interact(id)` is inspection only and never opens a quest transaction. `interactWorld(id)` is normal gameplay admission for human pointer release and agent interaction. Mob clicks inspect metadata without consuming world gameplay events.
- `showGeometry` and `revealHidden` are controlled by the corresponding labeled preview checkboxes. The latter explicitly overrides authored `hide=1` for inspection only.

Gameplay NPC records expose `{id,templateId,name,functionName,authority,kind,authored,info,interactionGeometryKnown,mode,canInteract,admissionPolicy}`. Authority is `offline-local-policy`; mode is `npc-local-interaction`. `canInteract():boolean` rechecks the current scene membership, rendered NPC artwork, hidden/alive state and `hooks.isBlocked(id)`. Cursor selection, world release and normal agent interaction share this admission; the previous invented 120×100 reach restriction is removed. Quest/UI confirmation rechecks the prebound callback after state/map changes. Inspector selections never invoke this hook. Original sounds are not dispatched merely for metadata selection; callbacks retain explicit error handling.

## Metadata schema and resource identities

`life` is JSON-compatible:

- `schemaVersion: 1`, `mode: "metadata-preview"`, `activationKnown: false`, `mapId`.
- `placements`: stable `id`, template key, kind, exact authored scalar/vector fields and exact WZ path. Missing `f`, `hide`, `mobTime`, `limitedname` remain missing. `x/y` are authored positions; `fh/cy/rx0/rx1` are retained without claiming live controller state.
- `templates`: keyed by `npc:<seven-digit original id>` or `mob:<seven-digit original id>`. Each retains original id, exact String.wz `name` and optional `func`, selected template info, `artworkHash`, action metadata, and lossless nonpixel source trees.
- Source-tree rows retain type, path, raw scalar/vector values, UOL text, and original canvas dimensions. They do not embed parent pointers or encoded pixels. `context.image` additionally records input hashes in the shared extraction provenance.
- Per-action metadata retains authored properties and every frame's original key, raw delay type/value (`null` denotes absent), normalized milliseconds, origin, width/height, and explicit `lt/rb` body rectangle or `null`.
- Templates additionally retain `defaultAction`, `artworkStatus` and original mob combat restrictions/type-0 action inputs. Missing stand uses an existing original fly/other action, not invented artwork; templates with no action canvases remain metadata-only. Missing optional foothold/range fields are preserved and their unsupported movement family is classified.
- Packaged `life.renderables[template]` supplies original per-template frames, atlas dependencies and presentation-only bounds; mobs are excluded from static region ownership.
- No new Entity/Frame/Part extension is required. All artwork actions use the existing contracts. `context.part` supplies lossless content-addressed artwork and original origin offsets. The life cache is scoped to the extraction context and keyed by original kind/id; identical pixel hashes share the existing atlas identity across templates and placements. `artworkHash` hashes the compiled action/part content, not an invented live actor id.

Limits are engineering bounds, not original constants: 4,096 placements per map, 8,192 templates per extraction context (expanded for the bounded portal closure), 128 action branches, 1,024 authored frames per action, 32,768 metadata nodes per source IMG, depth 64, 32 mob link hops. Malformed/cyclic input throws rather than truncating; missing unsupported action families remain classified. The original eight-map acceptance set contains no IMG links; UOL aliases resolve through the existing parser.

NPC image `info/link` redirects artwork once, while source NPC info and original-id String.wz lookup are retained (`006dce02`). Mob links form an iterative chain with cycle detection (`0067cf06`). Chained NPC artwork-cache semantics outside the acceptance set are not claimed as proved; source rows retain the exact link data.

## Recovered original consumers

Fresh evidence is under `docs/ghidra-ingame-life/`, generated read-only from the isolated `/tmp/maple-ingame-life.gpr` project using installed `analyzeHeadless` and `docs/tools/clientFocus.java` / `clientInstructions.java`.

| Original address        | Recovered behavior and boundary                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `00639b3d`              | Existing map preloader enumerates `life`, reads type/id, and loads NPC/mob templates. It does not establish live actors.                                                                                                                                                                                                                                                                                      |
| `00858603`              | Fresh decompilation enumerates map `life`; compares type to decoded string `0x573 = n`, skips nonzero `hide` (`0xc17`), and collects `x/y` (`0x3d1/0x3d2`) in a presentation collection. This is additional NPC map-presentation evidence, not spawn authorization. The surrounding minimap-related properties support a minimap interpretation; exact class naming remains an inference.                     |
| `008ebfde`              | **Not a Map life consumer.** The shared string id `life` appears in item/pet-duration formatting alongside `permanent`, water-of-life expiry, and `0x2a4 = DAYS OF MAGIC : %d`. Treating this xref as a spawn consumer would be incorrect.                                                                                                                                                                    |
| `0067cd28` → `0067cf06` | Mob template cache/load; resolves Mob IMG links and loads String/Mob names and template metadata, including `hideName`. No AI is derived from this loader.                                                                                                                                                                                                                                                    |
| `006dcb16` → `006dce02` | NPC original-id strings, optional function, hideName, authored interaction metadata, and linked artwork.                                                                                                                                                                                                                                                                                                      |
| `006d9993`, `006d9549`  | Pool/network-facing paths read ids, load templates, construct actors via `006d041e`, and initialize via `006d089a`. This reinforces that map metadata is not a replacement for live pool state.                                                                                                                                                                                                               |
| `006d2e07`              | Reads two packet bytes; updates the NPC action state and dispatches additional resource/speech data only under template/state conditions. Resource branch names alone do not reproduce this protocol.                                                                                                                                                                                                         |
| `006d263e`, `006d232a`  | Current action/motion low bit supplies facing; the Gr2D flip setter receives `bit == 0`. Mapping authored placement `f` into this live actor field is still unproved. Preview policy maps authored `f=0` to mirrored artwork and `f=1` to unmirrored; absent `f` retains artwork.                                                                                                                             |
| `0040db31`              | NPC action frame cache loads authored Canvas delay and optional zigzag. **NPC absent delay is 180 ms**, not the generic extractor's 120 ms. Disassembly `default-instructions.txt` shows `PUSH 0xb4` at `0040de3a`, `delay` id `0x15f8`, conversion call `00414d40` at `0040de89`, and stored result at `0040de8e`. No acceptance action has zigzag; special branches are selectable only as manual previews. |
| `006d5c9a` | NPC names/functions use original strings and honor `hideName`. Gameplay mob names have a separate recovered five-second local-hit lifetime and original hide/gauge gates, owned solely by `OfflineMobRenderer`; no persistent duplicate or diagnostic suffix is drawn. Typography/outline/spacing remain browser policies. |
| `006d267d`, `00664e35` | NPC layer is `B+29995+(layer*3000-group)*10`; ordinary mob layer is `B+29991+(layer*3000-group)*10`. `OfflineMobRenderer` now follows its current foothold, not the first instance's cached template depth. Native initially uncontacted plane7/group0 replaces the old invented plane0 fallback. Special-controller and boss overrides are retained separately in `ghidra-client/depth-actors.txt`, not claimed as implemented ordinary movement. |
| `00664559`, `00663fe2`  | Mob body selects cached facing rectangle, translates at current position, and optionally unions that **same current rectangle** at previous position. The swept rectangle is not a union with the preceding animation frame's body. `00663fe2` reads the actor direction low bit, with a template override.                                                                                                   |
| `0066936e`              | Mob action/canvas helper consumes original delay/body metadata. Fixed-30-ms mob/NPC actor updates remain documented in the earlier research; the life preview does not alter simulation cadence.                                                                                                                                                                                                              |
| `00531b8d` → `006d92d3` | The field cursor update selects NPC cursor 1 through the same NPC pool picker as left-button release, not artwork bounds or nameplate ancestry. |
| `006d92d3` → `006d3fde` / `006d4029` | First eligible live pool NPC whose translated dc rectangle **or** optional quest-indicator layer rectangle contains the pointer. The dc rectangle is not facing-mirrored. |
| `006dd584`–`006dd6f0` | Per-edge absent dc defaults are `left=-22`, `top=-65`, `right=22`, `bottom=0`. Authored overrides remain literal, including partial rectangles whose other edges use their native defaults. |
| `005299de` → `0094cf7a` → `0094fa8e` | Field button dispatch requires a local user and no global input blocker; `0x202` (`WM_LBUTTONUP`) picks the NPC and calls `0095fe9e`. `0x201` goes to `0094fa64`; `0x203` double-click goes to the separate `0094fbbf`, not NPC talk selection. |
| `0095fe9e` | Sends the selected NPC object ID plus the player's current short x/y, or enters the local quest branch. Neither this path nor pointer selection supplies the old browser 120×100 reach check; original server distance acceptance is not recovered here. |

Retained decoded string identities are corroborated by `docs/ghidra-client/decoded-strings.txt`. Geometry research remains in `docs/hitboxes.md` and `docs/ghidra-physics-hitboxes/`; current local incoming/outgoing combat is documented separately in [offline-combat.md](offline-combat.md), preserving the recovered/unrecovered authority distinction.

## Timing, visibility and geometry policies

Strict integer/string numeric delay conversion happens once during extraction. For example, Rina's `blink` aliases resolve the repeated string sequence `"150", "100", "1200"`. Maya's single stand frame has no authored delay and uses the newly recovered NPC 180-ms default. Explicit zero delay remains unsupported. Missing mob delay remains unsupported until its actual loader default is proved. An unsupported action retains all authored frame metadata but exposes only an untimed first-frame artwork preview, compatible with shared frame validation. **No acceptance action has unsupported timing.**

NPCs initially preview existing stand/fly/other original artwork; authoring flags do not authorize original spawn activation. Mob current action comes only from the fixed-tick offline field. Authored hidden/limited/inactive mobs remain inactive; reveal controls do not override gameplay visibility. NPC `hideName` still suppresses names. No event dates or missing scripts are invented.

Geometry namespaces remain separate:

- Mob body uses explicit per-frame `lt/rb`, mirrored about original anchor and translated into world pixels.
- Swept mob body uses the same current-frame rectangle at previous/current simulation positions. The inspector observes this fixed-tick sweep, never an elapsed-render-frame approximation or a stale pre-unload position.
- NPC `dcLeft/dcTop/dcRight/dcBottom` are original pointer-selection geometry, never physical bodies or damage geometry. Each absent edge now uses its recovered default. The rectangle translates at the current rendered NPC anchor without flipping, and right/bottom boundaries are excluded. Inspector selection remains nondamaging and cannot initiate quests.
- Authored `fh` and optional `cy/rx0/rx1` are displayed when present. The gameplay mob owner separately projects onto the exact original finite segment and follows its contiguous links under documented local patrol rules.
- `playerFootInsideBody` reports only point-in-body inspection of the existing avatar foot position. It is not avatar receiver collision, contact damage, or a hit decision.

`update` mutates prebuilt graphics transforms, text positions and rectangle slots. No arrays, objects, strings, closures or Graphics command lists are created in the hot loop. Pointer callbacks are prebound at construction; region membership/listener wiring occurs only in `refresh`. Preview action and elapsed phase survive region unload/rebind, explicitly a browser policy. All overlays, labels, handlers and DOM controls are owned and destroyed by LifeSystem; no texture ownership path is duplicated.

### NPC world target and admission

Read-only recovery is appended to `docs/ghidra-ingame-life/consumers.txt` under **NPC CURSOR / RELEASE RECOVERY 2026-09-09**, from `Maplestory_UNPACKED.exe` SHA-256 `1198fa57ca5a7c489bae43ec13c69681d9cabe0f96762f3dc0357facf2e7d4df` in `/tmp/maple-ingame-life`, with `-readOnly -noanalysis`. The exports include both cursor/click callers, the picker, dc defaults, translation, optional quest-indicator geometry and the separate double-click handler.

LifeSystem owns one nonvisual Pixi hit target per NPC at the original dc world rectangle. NPC artwork, text and inspector geometry cannot impersonate that target. Hit testing rechecks current admission, so a cached target immediately stops authorizing interaction after membership replacement, destruction, hidden/reveal state, absent rendered artwork, death or a blocking transition. NPC event delivery uses primary left `pointerup`: a preceding press on the same entity is not required, and no additional NPC double-click handler is installed. Rejected world events do not open the inspector or stop propagation. Inspector-only mob selection also does not consume gameplay propagation; eligible NPC targets have precedence over inspection artwork.

`hooks.isBlocked(id)` is supplied by the integration owner. It must reject profile/map transitions, carried UI payloads and unrelated modals, while allowing the already-open same-NPC dialogue to recheck its own `canInteract` closure. The shared admission calls `hooks.isBlocked()` **without an ID** for a new world target/cursor/normal-agent interaction, so even the current NPC modal blocks new activation. The main UI also blocks new world pointer/cursor activity while modal. This keeps quest continuation from invalidating itself simply because its own panel opened.

The native picker rejects NPC actor `+0x134 == 0` (network visibility), `+0x144 != 0` (condition-hidden state), and template `+0xc4 != 0` (`imitate`, retained UTF-16 key at `00afcad0`) without its separate avatar at actor `+0x8c`. The offline field has no original spawn/controller or imitation-avatar authority: it rejects authored-hidden, nonresident, nonrendered and imitation-only records instead of manufacturing that state. Player death/modal gates are explicit shared offline admission, not claims about a recovered original server permission check.

Remaining boundaries: `006d4029` additionally accepts the actual optional quest-indicator layer at actor `+0xec`, created/removed by `006d1779`. That indicator is not currently rendered, so no invisible or fabricated quest-marker target is added. Native pool order comes from live network actors; the offline tie-breaker is the existing authored placement order, independent of artwork draw order. Original conditional NPC visibility, server distance/rejection behavior, quest-marker presentation and original-runtime visual timing remain unproved.

`client/test/life-interaction.test.js` covers dc edges/facing, exact target identity, release routing, inspection separation, stale/hidden/dead/modal admission and presentation-gate lifecycle using production LifeSystem/Pixi objects. The retained147-test milestone and its native scenarios belong to the historical [expanded acceptance](ingame-validation/expanded/report.json); final source gates and replays are indexed separately in [validation](validation.md).

## Depth, floor origins and keyboard Talk

The [origin/layer inspector](../client/tools/inspect-origins.js) was executed directly on five original maps; the [retained JSON](ghidra-client/origin-layer-probe.json) contains69 NPC placements and51 backgrounds. It distinguishes authored map-editor `y`, optional `cy`, exact finite foothold height, canvas `origin` and opaque artwork bottom. Sixty-seven NPC placements were more than one pixel above their foothold if editor `y` was treated as final standing height. Maya is authored at`(-17,36)` on floor38, with origin`(23,77)` and artwork bottom0 relative to that origin; Kyrin is authored at`(-227,137)` on floor150, with bottom2. Aqua NPC9250023 has its original bottom at−42 and remains visibly floating; no bounding-box or name-height correction is applied.

NPC initialization `006d089a -> 009c1d70 -> 009b12a8/009b1553` supplies the original foothold and derives clamped contact distance. Extraction now positions an NPC with a valid finite floor at`x=clamp(authoredX,x1,x2)`, `y=y1+(x-x1)*(y2-y1)/(x2-x1)`, then performs the shared signed-integer world projection. Authored `y/cy/fh` stay intact in metadata. Missing/wall contact stays authored and is not presented as grounded. `LifeSystem.snapshot()` now exposes actual current anchor/depth and original current-frame canvas geometry for direct comparison.

Original `005f1775/005f17bb` attaches name/function overlays to their actor group with relative z2; it separately attaches their origin to the unflipped actor vector. Player/NPC/mob labels now respect higher map objects/fronts together with their actors instead of escaping into the diagnostic always-front root. Avatar RGB modulation stays on artwork sprites rather than tinting independent names. Ordinary dynamic depth uses renderer mutation serial ties, not world Y sorting; see [the recovered ordering equations](client-evidence.md#map-draw-ordering). Initial static equal-depth ordering remains the explicit authored extraction-order policy.

`LifeSystem.talkNearest():boolean` implements Main's recovered keyboard-Talk consumer `0094f2ed -> 006d9390`: it expands each translated, unflipped dc rectangle by the union of`(200,82)`, `(82,200)`, `(141,141)` horizontal/vertical margins. The latter values derive from the original float32`1.4140000343322754` at`00afc9f0`, using truncation, not Euclidean-radius guessing. Right/bottom edges remain excluded. Among eligible rectangles containing the player origin it selects minimum squared origin distance, retaining authored pool order on equality, and invokes the same`interactWorld` admission as pointer interaction. Dead/blocked/hidden/nonresident/imitation guards are shared. The native extra template`+0x44` gate is **talkMouseOnly**: original loader`006dd71d..754`, retained`consumers.txt4977..4992`, reads decoded string`0x68b` and writes that field. Keyboard Talk rejects it; pointer interaction remains available.

This slice executed the original-WZ inspector and numerical smokes with actual production Pixi scene/animation objects (depth insertion/mutation, independent120/120-ms expression-frame boundaries and expiry). A separate keyboard-selection calculation used the production `talkNearest` method with admitted candidate inputs: it selected the closer non-mouse-only NPC, skipped a mouse-only candidate at the player origin, and excluded right/bottom boundary points. That calculation does **not** claim browser routing or full admission coverage. No project build, lint, tests, release extraction or integrated native-browser acceptance was run by this worker. Main owns keyboard Talk, floor/overlap browser scenarios and consolidated validation.

## Coverage and exercised proof

`docs/ghidra-ingame-life/extraction-probe.json` is an actual original-input extraction/decode run, not estimated inventory:

| Map       | NPC placements | Mob placements |
| --------- | -------------: | -------------: |
| 100000000 |             30 |              0 |
| 100000001 |              1 |              0 |
| 103040000 |              7 |              0 |
| 108000500 |              1 |             15 |
| 120000000 |             15 |              0 |
| 200090500 |              0 |              0 |
| 211040000 |              0 |             34 |
| 230000000 |              9 |              0 |
| **Total** |         **63** |         **49** |

All **112 placements, 53 NPC templates and 3 mob templates** decoded successfully through the original parser and lossless Canvas decoder. The probe found **529 distinct pixel hashes**, **22,354,884 bytes of unique decoded RGBA**, no template links in this acceptance set, and no unsupported action timing. This number is extraction payload size, **not live resident GPU memory**. Full action metadata includes special branches without activating them automatically.

An isolated native-browser smoke used the installed Pixi ES module (no build), the actual LifeSystem/EntityAnimation modules, and exact decoded Maya/Hector/White Fang artwork. It did not use a mock renderer. Exercised:

1. Maya displayed at original authored anchor with `Maya` and explicit preview label; a real pointer click produced one server-unavailable NPC interaction record with template `1012101`, without inventing dc geometry.
2. Actual map selector input replaced Maya with all 34 authored 211040000 mob previews; the old owner left exactly one preview DOM panel after replacement.
3. Actual action selector input chose Hector `move`; the geometry checkbox displayed per-frame bodies, selected sweep, and authored foothold/contact/range overlays. The retained image is `docs/ghidra-ingame-life/native-browser-preview.png`.
4. A separate inspection experiment translated Hector 20 pixels without adding movement logic. Current body was `[-341,-100]..[-239,-45]`; sweep was `[-361,-100]..[-239,-45]`, preserving the same-frame union rule.
5. A membership-removal/rebind experiment reported resident false and inactive geometry while absent, then restored `move` and the same displayed frame. No world texture loader was added to the production life owner.

The isolated probe bypassed production streaming to exercise this domain independently. Integrated native evidence now lives in [life/report.json](ingame-validation/life/report.json), with twenty screenshots and exact action records. It covers Maya/Kyrin identity, Hector/Octo action/body previews, hidden/name suppression, natural region residency, control focus and map-owner replacement. Native NPC close buttons exposed the shared UI pointer-reparenting bug; [fixed-ui-npc-close.json](ingame-validation/fixed-ui-npc-close.json) confirms Main's correction using both actual close controls. Dense static-preview names can overlap; no live-spawn/AI/font/depth parity is claimed. Far-right Octo traversal and nonzero live actor sweeps were not exercised.

## Required Windows/reference captures and server boundary

Expand original-runtime capture requests with:

- Maya and Thompson: anchored artwork, native font, name/function layout, animation frame timestamps, NPC click and keyboard interaction behavior.
- Kyrin on 108000500 against ordinary `f=0` NPCs: correlate authored facing with spawn/controller packet direction and Gr2D flip.
- Hidden Bush/Maple TV/Ice Piece: distinguish map `hide`, template `hideName`, click geometry and event activation. Capture `limitedname` availability with event/server context.
- Rina blink/smile/hair: verify UOL animation cadence, special-action selection protocol, absence/zero delay behavior and any action-loop handling.
- Hector/White Fang: compare frame-dependent body rectangles and direction; capture actual actor depth, contact/invulnerability handling and previous-position sweep boundaries without inferring combat from rectangles.
- OctoPirate: capture special `mobType=4`, `mobTime=-1` placements and live controller packets; do not generalize generic ground AI from this template.
- Record spawn/despawn ids, controller assignment and action packets if server-authoritative reconstruction is later authorized. The supplied offline assets cannot establish these states.

No original visual parity, live-spawn fidelity or server-compatible interaction protocol is claimed. The recovered supported one-handed-sword generator and physical evasion are narrower client calculations, documented in [combat](offline-combat.md), not evidence of complete original combat/server behavior. Incoming magnitude, autonomous mob decisions and progression remain explicit local authority.
