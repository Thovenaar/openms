# Official Debian Bookworm PostgreSQL image; do not silently float the version.
# PostgreSQL 18 persists PGDATA under /var/lib/postgresql/18/docker.
# Mount the named volume at /var/lib/postgresql (not the pre-18 data mount).
FROM docker.io/library/postgres:18.6-bookworm
